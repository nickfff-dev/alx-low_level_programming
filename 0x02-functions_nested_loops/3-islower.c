#include "main.h"

/**
 * _islower - checks for lowercase character
 * Return: 1 if c is lowercase, 0 if uppercase
 */
int _islower(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return 0
	} else
	{
	
		return 1
	}
}
