#include "main.h"
/**
 * print_numbers - prints digits 0-9
 */
void print_numbers(void)
	char c;
	for (i ='0'; i <= '9'; i++)
{

	if (i != '2' && i != '4')
	{
	
		_putchar (i);
	}
}
	_putchar('\n');
}
