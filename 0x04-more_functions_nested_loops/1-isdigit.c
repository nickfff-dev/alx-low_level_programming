#include "main.h"
/**
 * _isdigit - takes a parameter andd checks if it is a digit
 * @c: the variable to be cheecked
 * Return: always 0
 */

int _isdigit(int c)
{

	return (c >= '0' && c <= '9');
}
